diff --git a/utils/doc.go b/utils/doc.go
new file mode 100644
index 0000000..c36d787
--- /dev/null
+++ b/utils/doc.go
@@ -0,0 +1,7 @@
+// Package utils provides shared infrastructure used by api-tools scrapers,
+// parsers, and uploaders.
+//
+// The package includes browser-session helpers, retry and collection helpers,
+// verbose logging, regular-expression constants, PDF extraction, Gemini schema
+// generation, and Nebula API cache access.
+package utils
diff --git a/utils/methods.go b/utils/methods.go
index addb000..3c70b64 100644
--- a/utils/methods.go
+++ b/utils/methods.go
@@ -1,4 +1,3 @@
-// Package utils provides shared helpers for scraping, parsing, and uploading workflows.
 package utils
 
 import (
@@ -322,11 +321,15 @@ func ConvertFromInterface[T string | float64](value any) *T {
 	return nil
 }
 
+// LinkResult contains the visible text and destination URL extracted from an
+// HTML anchor node.
 type LinkResult struct {
 	Text string
 	Href string
 }
 
+// ExtractTextAndHref extracts the text content and href attribute from each
+// anchor node. It panics when chromedp cannot read a node's text content.
 func ExtractTextAndHref(nodes []*cdp.Node, chromedpCtx context.Context) []LinkResult {
 	output := []LinkResult{}
 	var err error
diff --git a/utils/parser.go b/utils/parser.go
index b0ae6f0..2ac1553 100644
--- a/utils/parser.go
+++ b/utils/parser.go
@@ -1,4 +1,3 @@
-// Package utils provides shared helpers for parsing workflows.
 package utils
 
 import (
@@ -20,10 +19,9 @@ import (
 	"google.golang.org/genai"
 )
 
-// Read the text from the first n pages of a PDF
-// Using external program pdftotext
-// Code requires having pdftotext installed: https://www.xpdfreader.com/pdftotext-man.html
-// apt-get install -y poppler-utils
+// ReadPdf extracts text from a PDF using the external pdftotext command. When
+// lastPage is positive, extraction is limited to the first lastPage pages.
+// The command must be installed separately, for example through poppler-utils.
 func ReadPdf(path string, lastPage int) (string, error) {
 	cmd := exec.Command("pdftotext", "-raw", path, "-")
 	if lastPage > 0 {
@@ -42,7 +40,9 @@ func ReadPdf(path string, lastPage int) (string, error) {
 	return out.String(), nil
 }
 
-// Check cache for a response to the same prompt
+// CheckCache returns a cached response from a Nebula API storage bucket. An
+// unparseable metadata response is treated as a cache miss and returns an empty
+// string with no error.
 func CheckCache(hash string, apiBucket string) (string, error) {
 	apiUrl, apiKey, apiStorageKey, err := getNebulaKeys()
 	if err != nil {
@@ -96,12 +96,15 @@ func CheckCache(hash string, apiBucket string) (string, error) {
 	return string(body), nil
 }
 
-// Store Gemini client to only create once
+// once ensures the shared Gemini client is initialized at most once.
 var once sync.Once
+
+// geminiClient stores the shared Vertex AI-backed Gemini client.
 var geminiClient *genai.Client
 
-// Create client only once
-// Auth is from GOOGLE_GENAI_USE_VERTEXAI, GOOGLE_CLOUD_PROJECT and GOOGLE_APPLICATION_CREDENTIALS environment variables and service account JSON which is created from GEMINI_SERVICE_ACCOUNT
+// GetGeminiClient initializes and returns the shared Gemini client. It writes
+// the GEMINI_SERVICE_ACCOUNT value to GOOGLE_APPLICATION_CREDENTIALS and uses
+// the Vertex AI backend.
 func GetGeminiClient() *genai.Client {
 	once.Do(func() {
 		// Create JSON file
@@ -132,6 +135,9 @@ func GetGeminiClient() *genai.Client {
 	return geminiClient
 }
 
+// StructToSchema converts a Go reflection type into a Gemini response schema.
+// Pointer fields are marked nullable, and non-pointer struct fields with JSON
+// tags are marked required.
 func StructToSchema(t reflect.Type) *genai.Schema {
 	// Handle pointers
 	isNullable := false
@@ -192,7 +198,7 @@ func StructToSchema(t reflect.Type) *genai.Schema {
 	return schema
 }
 
-// Upload AI response to cache
+// SetCache stores an AI response in a Nebula API storage bucket.
 func SetCache(hash string, result string, apiBucket string) error {
 	apiUrl, apiKey, apiStorageKey, err := getNebulaKeys()
 	if err != nil {
@@ -219,7 +225,8 @@ func SetCache(hash string, result string, apiBucket string) error {
 	return nil
 }
 
-// Get all the keys to access the Nebula API storage routes
+// getNebulaKeys reads the URL and credentials required by Nebula API storage
+// routes from the environment.
 func getNebulaKeys() (string, string, string, error) {
 	apiUrl, err := GetEnv("NEBULA_API_URL")
 	if err != nil {
